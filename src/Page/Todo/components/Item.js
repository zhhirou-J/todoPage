import Edit from "./Edit";

const Item = ({key, task, category, date }) => {
    return (
      <div>
        {/* <p>{key}</p>
        <p>{task}</p>
        <p>{category}</p>
        <p>{date}</p> */}
      </div>
    );
  };
  
  export default Item;
  
  //getitem 拿瀏覽器的資料